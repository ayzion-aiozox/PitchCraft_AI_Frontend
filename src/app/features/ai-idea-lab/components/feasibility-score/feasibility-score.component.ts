import { ChangeDetectionStrategy, Component } from '@angular/core';

/** Feasibility score display for AI Idea Lab — shell for future wiring. */
@Component({
  selector: 'app-feasibility-score',
  standalone: true,
  templateUrl: './feasibility-score.component.html',
  styleUrls: ['./feasibility-score.component.scss'],
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class FeasibilityScoreComponent {}
