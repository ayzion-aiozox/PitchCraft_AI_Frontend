/** Re-export shared dashboard types — single source of truth with `DashboardService`. */
export type {
  TrendData,
  KPI,
  FunnelStage,
  ActionItem,
  ChatMessage,
  DashboardState,
} from '../../../shared/models/dashboard.model';
