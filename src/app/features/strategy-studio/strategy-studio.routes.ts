import { Routes } from '@angular/router';
import { StrategyBuilderComponent } from './strategy-builder.component';

export const STRATEGY_STUDIO_ROUTES: Routes = [
    { path: '', component: StrategyBuilderComponent }
];