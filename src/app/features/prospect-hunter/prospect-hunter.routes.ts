import { Routes } from '@angular/router';
import { DiscoveryConsoleComponent } from './discovery-console.component';

export const PROSPECT_HUNTER_ROUTES: Routes = [
    { path: '', component: DiscoveryConsoleComponent }
];