import { Routes } from '@angular/router';
import { IntegrationStationComponent } from './integration-station.component';

export const INTEGRATIONS_ROUTES: Routes = [
  { path: '', component: IntegrationStationComponent },
];
