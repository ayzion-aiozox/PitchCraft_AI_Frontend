// To run with staging urls, use command
// ng serve --configuration staging
// To Build, use command
// ng build --configuration staging --base-href=/stg/corporate-portal/webapp/

import stagingConfig from '../configurations/stagingConfig.json';

export const environment = stagingConfig;
